<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

        <item name="color">@color/nameViewText</item>

        <item name="android:windowNoTitle">true</item>
        <item name="windowActionBar">false</item>

    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />


    <!--<style name="MyCheckBox" parent="Theme.AppCompat.Light">-->
        <!--<item name="colorControlNormal">@android:color/holo_green_light</item>-->
        <!--<item name="colorControlActivated">@android:color/holo_orange_dark</item>-->
    <!--</style>-->

    <!-- Theme CheckBox. -->
    <style name="MyCheckBox" parent="Theme.AppCompat.Light">
        <item name="colorControlNormal">@color/checkBoxGreen</item>
        <item name="colorControlActivated">@color/checkBoxYellow</item>
    </style>

    <!-- Theme Calendar and Time. -->
    <style name="DialogTheme" parent="Theme.AppCompat.Light.Dialog">
        <item name="colorAccent">@color/checkBoxYellow</item>
        <item name="android:colorPrimary">@color/colorPrimary</item>
        <item name="android:colorPrimaryDark">@color/colorAccent</item>
        <item name="android:colorAccent">@color/checkBoxGreen</item>
    </style>

</resources>
